rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Mandi Business Management rules - allow read/write for all application collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

